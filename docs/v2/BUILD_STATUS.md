# ReferralRail v2 build status

Branch: referralrail-v2
Network: Studio Next / Studionet Dev
Chain: 61997
RPC: https://studio-dev.genlayer.com/api

The active v2 architecture uses GITHUB_PR and PUBLIC_WEB. PUBLIC_WEB is universal bounded HTTPS evidence. X and Medium are frontend presets only and are not protocol modes.

This document is updated during the final v2 gate. A deployment is current only when deployment/v2-61997.json and deployment/v2/live-evidence.json point to the same source generation.

Required final gates include contract tests and lint, frontend typecheck/build, SDK and MCP build/test/pack checks, clean STDIO E2E, live GitHub success, live negative, live inconclusive retry, multi-position capacity proof, PUBLIC_WEB success, browser validation, and final Vercel preview validation.
