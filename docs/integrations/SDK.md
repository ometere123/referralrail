# SDK

`@referralrail/sdk` targets the finalized 61997 deployment. It exposes typed reads, role-aware available actions, safe writes, centralized fee policy, explicit finalization, execution checks, and finalized state readback. The SDK does not publish itself or modify the frontend.
