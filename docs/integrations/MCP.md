# MCP

`@referralrail/mcp` is a small STDIO server built on the SDK. It is read-only by default. Set both `REFERRALRAIL_PRIVATE_KEY` and `REFERRALRAIL_WRITE_ENABLED=true` for write authority. Do not place secrets in tool arguments or configuration committed to Git.
