# Deployment evidence

This directory intentionally contains no claimed live address or transaction hash yet.

`deploy/001_deploy_referralrail.ts` writes `61997.json` only after real finalized deployment/configuration transactions have succeeded and the protocol config has been read back.

After live product testing, store real interaction evidence in `live-evidence.json`. Never replace these files with example hashes that could be mistaken for live proof.
